# Generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=****

# The following keys below are automatically created and
# added to your environment when you deploy on Vercel

# Instructions to create an AI Gateway API key here: https://vercel.com/ai-gateway
# API key required for non-Vercel deployments
# For Vercel deployments, OIDC tokens are used automatically
# https://vercel.com/ai-gateway
AI_GATEWAY_API_KEY=****


# Instructions to create a Vercel Blob Store here: https://vercel.com/docs/vercel-blob
BLOB_READ_WRITE_TOKEN=****

# Instructions to create a PostgreSQL database here: https://vercel.com/docs/postgres
POSTGRES_URL=****


# Instructions to create a Redis store here:
# https://vercel.com/docs/redis
REDIS_URL=****

# ============================================================================
# iFlow CLI SDK Configuration
# ============================================================================

# iFlow workspace directory (where all user workspaces are stored)
# Default: /var/workspaces
# Each workspace will be created as: /var/workspaces/iflow_cli_{workspace_uuid}
IFLOW_WORKSPACE_DIR=/var/workspaces

# iFlow API key (if required by your iFlow CLI setup)
# Obtain from: https://iflow.ai or your iFlow CLI configuration
IFLOW_API_KEY=****

# Default AI model for new workspaces
# Options: MiniMax-M2 | Qwen3-Coder-Plus | DeepSeek-V3.2 | GLM-4.6 | Kimi-K2-0905
# Default: MiniMax-M2
IFLOW_DEFAULT_MODEL=MiniMax-M2

# Default permission mode for new workspaces
# Options: default | autoEdit | yolo | plan
# - default: Requires confirmation for each operation
# - autoEdit: Auto-executes edit operations
# - yolo: Auto-executes all operations
# - plan: Creates execution plan first
# Default: yolo
IFLOW_DEFAULT_PERMISSION_MODE=yolo
